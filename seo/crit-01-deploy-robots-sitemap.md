# #1 Deploy robots.txt + sitemap.xml

**Priority:** CRITICAL
**Status:** `done`
**Effort:** Low — 15 min
**Impact:** Very High
**Tags:** TECHNICAL, GEO, CRAWLABILITY

## Problem

`robots.txt` and `sitemap.xml` were generated by the sitemap subagent and written to the repo root but have not yet been committed and deployed. Without them:
- No `Sitemap:` directive for crawlers
- No AI-training bot opt-out
- Pages may go undiscovered or be slow to index

## Fix

1. Verify `robots.txt` and `sitemap.xml` exist in the repo root
2. Commit both files
3. Deploy (push to `main`)
4. Verify live at `https://shierlytherapy.com.au/robots.txt` and `/sitemap.xml`

## Notes

Proceed to task [#2](crit-02-submit-sitemap-gsc.md) (submit sitemap in Google Search Console) immediately after deploy.
